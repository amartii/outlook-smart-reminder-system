# Flask
SECRET_KEY=cambia-esto-por-una-clave-secreta

# Base de datos (SQLite local, no cambiar)
DATABASE_URL=sqlite:///agent.db

# Intervalos del scheduler (opcional, valores por defecto)
# SYNC_CONTACTS_INTERVAL_HOURS=6
# CHECK_RESPONSES_INTERVAL_MINUTES=15
# SEND_REMINDERS_INTERVAL_HOURS=1
